<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>編集画面</title>
</head>

<body>
    <div class="container mt-5">
        <h1 class="mb-4">商品情報編集画面</h1>
        <form method="POST" action="/product/<%= singleProduct.id %>?_method=PATCH">
            <div class="mb-3">
                <label for="productname" class="form-label">商品名</label>
                <input type="text" class="form-control" id="productname" placeholder="商品名" name="productname"
                    value="<%= singleProduct.name %>">
            </div>
            <div class="mb-3">
                <label for="price" class="form-label">値段</label>
                <input type="number" class="form-control" id="price" value="<%= singleProduct.price %>" name="price">
            </div>
            <div class="mb-3">
                <label for="category" class="form-label">カテゴリ</label>
                <select class="form-select" aria-label="Default select example" id="category" name="category">
                    <option value="野菜" <%=singleProduct.category==="野菜" ?"selected" :""; %>>野菜</option>
                    <option value="果物" <%=singleProduct.category==="果物" ?"selected" :"" ;%>>果物</option>
                    <option value="乳製品" <%=singleProduct.category==="乳製品" ?"selected" :""; %>>乳製品</option>
                </select>
            </div>
            <button class=" btn btn-primary">更新</button>
            <!-- typeでボタンを指定しないとフォームを送信してしまう。 -->
            <button type="button" class="btn btn-secondary" onclick="location.href='/products'">戻る</button>
        </form>
    </div>
    </form>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>